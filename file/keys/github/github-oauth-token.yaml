id: github-oauth-token

info:
  name: Github OAuth Access Token
  author: tanq16
  severity: high
  tags: file,keys,token,github
file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "gho_.{36}"

