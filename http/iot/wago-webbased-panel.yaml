id: wago-webbased-panel
info:
  name: WAGO WebBased Management - Panel
  author: biero-el-corridor
  severity: info
  description: |
    WAGO WebBased Management was discovered.
  metadata:
    # auto-generated by nuclei-sync — please review before merging
    runzero-match: any(each(service["html.titles"]), {# matches "(?i)WAGO Ethernet Web-based Management"})
    max-request: 1
    shodan-query: title:"WAGO Ethernet Web-based Management"
    verified: true
  tags: discovery,iot,login,panel,wago
http:
  - matchers:
      - condition: and
        part: body
        type: word
        words:
          - Web-based Management
          - login
      - status:
          - 200
        type: status
    matchers-condition: and
    method: GET
    path:
      - '{{BaseURL}}/wbm/index.php'
