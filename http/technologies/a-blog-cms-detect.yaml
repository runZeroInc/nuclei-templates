id: a-blog-cms-detect

info:
  name: a-blog cms - Detect
  author: Shivam Kamboj
  severity: info
  description: |
    Detects the presence of a-blog cms, a Content Management System.
  reference:
    - https://www.a-blogcms.jp/
  metadata:
    runzero-match: service["http.body"] matches "(?i)a-blog cms"
    verified: true
    max-request: 1
    shodan-query: html:"a-blog cms"
  tags: tech,discovery,cms,a-blog-cms

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    redirects: true

    matchers:
      - type: word
        part: body
        words:
          - "a-blog cms"
          - "acms-js"
        condition: and

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '(?i)content="a-blog cms (?:Ver\.|v)?([0-9.]+)'