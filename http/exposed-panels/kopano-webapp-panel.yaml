id: kopano-webapp-panel

info:
  name: Kopano WebApp Login Panel - Detect
  author: righettod
  severity: info
  description: Kopano WebApp login panel was detected.
  reference:
    - https://kopano.com/
  classification:
    cpe: cpe:2.3:a:kopano:webapp:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 1
    vendor: kopano
    product: webapp
    shodan-query: http.title:"kopano webapp"
    fofa-query: title="kopano webapp"
    google-query: intitle:"kopano webapp"
    runzero-match: any(each(service["html.titles"]), {# matches "(?i)Kopano WebApp"})
  tags: panel,kopano,login,detect

http:
  - method: GET
    path:
      - "{{BaseURL}}/webapp/"

    host-redirects: true
    max-redirects: 2
    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains_any(body, "<title>Kopano WebApp", "content=\"Kopano WebApp")'
        condition: and

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '\?kv([0-9.]+)"'

