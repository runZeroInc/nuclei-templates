id: privategpt-detect

info:
  name: PrivateGPT - Detect
  author: ritikchaddha
  severity: info
  description: |
    PrivateGPT panel has been detected.
  reference:
    - https://github.com/zylon-ai/private-gpt
  metadata:
    verified: true
    max-request: 1
    product: private-gpt
    shodan-query: html:"private gpt"
    runzero-match: service["http.body"] matches "(?i)private gpt"
  tags: panel,privategpt,detect

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - 'alt=PrivateGPT'
          - 'title":"My Private GPT'
          - 'content="My Private GPT'
        condition: or

      - type: status
        status:
          - 200

