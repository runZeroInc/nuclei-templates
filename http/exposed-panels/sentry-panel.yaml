id: sentry-panel
info:
  name: Sentry Login Panel
  author: righettod
  severity: info
  description: |
    Sentry login panel was detected.
  reference:
    - https://sentry.io/
  classification:
    cpe: cpe:2.3:a:sentry:sentry:*:*:*:*:*:*:*:*
  metadata:
    runzero-match: any(each(service["html.titles"]), {# matches "(?i)login \\| sentry"})
    fofa-query: title="login | sentry"
    google-query: intitle:"login | sentry"
    max-request: 1
    product: sentry
    shodan-query:
      - http.title:"Login | Sentry"
      - http.title:"login | sentry"
    vendor: sentry
    verified: true
  tags: discovery,login,panel,sentry
http:
  - extractors:
      - group: 1
        part: body
        regex:
          - (?i)"current":\s*"([0-9a-z.-]+)"
        # digest: 4b0a004830460221008bb3c66d6381523b0a0eaa32985566f168985f37f32515ddfe608c80b949139e022100c056685d8756922ca34869f912cdc70e5d507322fd4fe136d4b84ba6eb174330:922c64590222798bb761d5b6d8e72950

        type: regex
    matchers:
      - condition: and
        dsl:
          - status_code == 200
          - contains(body, "/sentry/") && contains(body, "Login")
          - regex("(?i)<title>[^<]*sentry[^<]*</title>", body)
        type: dsl
    max-redirects: 3
    method: GET
    path:
      - '{{BaseURL}}'
    redirects: true
