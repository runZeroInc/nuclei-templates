id: addonfinance-portal

info:
  name: AddOnFinance Portal - Exposed Panel
  author: ritikchaddha
  severity: info
  description: AddOnFinance Portal panel was detected.
  metadata:
    runzero-match: |-
        any(each(service["html.titles"]), {# matches "AddOnFinancePortal"})
    verified: "true"
    max-request: 1
    shodan-query: title:"AddOnFinancePortal"
  tags: panel,addon,finance,detect

http:
  - method: GET
    path:
      - '{{BaseURL}}'

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - 'AddOnFinancePortal</title>'

      - type: status
        status:
          - 200

