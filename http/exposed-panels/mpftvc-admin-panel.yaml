id: mpftvc-admin-panel

info:
  name: MPFTVC Admin Login Panel - Detect
  author: Hardik-Solanki,gmeghab
  severity: info
  description: MPFTVC admin login panel was detected.
  classification:
    cvss-metrics: CVSS:3.0/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:N
    cwe-id: CWE-200
  metadata:
    verified: true
    max-request: 1
    shodan-query: title:"AdminLogin - MPFTVC"
    runzero-match: any(each(service["html.titles"]), {# matches "(?i)AdminLogin - MPFTVC"})
  tags: panel,mpftvc,admin

http:
  - method: GET
    path:
      - '{{BaseURL}}'

    host-redirects: true
    max-redirects: 2

    matchers-condition: and
    matchers:
      - type: word
        words:
          - "AdminLogin - MPFTVC"
          - "Admin-Login: MPFTVC"
        condition: or

      - type: status
        status:
          - 200

