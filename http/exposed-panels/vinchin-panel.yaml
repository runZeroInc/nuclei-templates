id: vinchin-panel

info:
  name: Vinchin Backup & Recovery Panel - Detect
  author: pussycat0x
  severity: info
  description: Vinchin Backup & Recovery login panel was detected.
  classification:
    cwe-id: CWE-200
    cpe: cpe:2.3:a:vinchin:vinchin_backup_and_recovery:*:*:*:*:*:*:*:*
  metadata:
    runzero-match: service["http.body"] matches "(?i)VinChin"
    verified: true
    max-request: 1
    vendor: vinchin
  tags: panel,vinchin,login,discovery

http:
  - method: GET
    path:
      - '{{BaseURL}}'

    host-redirects: true

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - 'Vinchin Backup & Recovery'

      - type: status
        status:
          - 200

