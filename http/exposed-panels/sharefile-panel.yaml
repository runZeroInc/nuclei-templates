id: sharefile-panel
info:
  name: Sharefile Login - Panel
  author: irshad ahamed
  severity: info
  description: |
    ShareFile is a cloud-based file sharing and collaboration platform that provides secure access to files from anywhere.
  reference:
    - https://www.sharefile.com/
  classification:
    cpe: cpe:2.3:a:citrix:sharefile:*:*:*:*:*:*:*:*
  metadata:
    runzero-match: any(each(service["html.titles"]), {# matches "(?i)sharefile login"})
    fofa-query: title="sharefile login"
    google-query: intitle:"sharefile login"
    max-request: 1
    product: sharefile
    shodan-query:
      - title:"ShareFile Login"
      - http.title:"sharefile login"
    vendor: citrix
    verified: true
  tags: citrix,detect,discovery,login,panel,sharefile
http:
  - matchers:
      - condition: and
        type: word
        words:
          - ShareFile Login
          - password
      - status:
          - 200
        type: status
    matchers-condition: and
    method: GET
    path:
      - '{{BaseURL}}/Authentication/Login'
