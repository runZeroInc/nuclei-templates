id: scribble-diffusion-panel

info:
  name: Scribble Diffusion Panel - Detect
  author: rxerium
  severity: info
  description: |
    A tool to turn your rough sketch into a refined image using AI.
  reference:
    - https://scribblediffusion.com/
    - https://github.com/replicate/scribble-diffusion
  metadata:
    runzero-match: any(each(service["html.titles"]), {# matches "(?i)Scribble Diffusion"})
    verified: true
    max-request: 1
    shodan-query: title:"Scribble Diffusion"
  tags: panel,scribble,detect,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers-condition: and
    matchers:
      - type: word
        words:
          - '<title>Scribble Diffusion'
          - 'content="Scribble Diffusion'
        condition: or

      - type: status
        status:
          - 200

