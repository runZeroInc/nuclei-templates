id: vaultwarden-panel

info:
  name: Vaultwarden Login Panel - Detect
  author: righettod
  severity: info
  description: |
    Vaultwarden products was detected.
  reference:
    - https://github.com/dani-garcia/vaultwarden
  metadata:
    verified: true
    max-request: 1
    shodan-query: http.html:"vaultwarden"
    runzero-match: service["http.body"] matches "(?i)vaultwarden"
  tags: panel,vaultwarden,login

http:
  - method: GET
    path:
      - "{{BaseURL}}/#/login"

    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains(to_lower(body), "vaultwarden web</title>")'
        condition: and

