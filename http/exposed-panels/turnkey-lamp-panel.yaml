id: turnkey-lamp-panel

info:
  name: TurnKey LAMP Panel - Detect
  author: ritikchaddha
  severity: info
  description: |
    TurnKey LAMP Control Panel was detected.
  reference:
    - https://www.turnkeylinux.org/lamp
  metadata:
    verified: true
    max-request: 1
    shodan-query: title:"TurnKey LAMP"
    fofa-query: title="TurnKey LAMP"
    runzero-match: any(each(service["html.titles"]), {# matches "(?i)TurnKey LAMP"})
  tags: panel,login,turnkey,lamp,detect

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - "TurnKey LAMP</title>"

      - type: status
        status:
          - 200

