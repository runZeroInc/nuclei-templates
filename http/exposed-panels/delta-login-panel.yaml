id: delta-login-panel

info:
  name: Delta Controls Admin Login Panel - Detect
  author: gy741
  severity: info
  description: Delta Controls admin login panel was detected.
  classification:
    cvss-metrics: CVSS:3.0/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:N
    cwe-id: CWE-200
  metadata:
    max-request: 1
    shodan-query: http.html:"Delta Controls ORCAview"
    runzero-match: service["http.body"] matches "(?i)Delta Controls ORCAview"
  tags: panel,delta

http:
  - method: GET
    path:
      - "{{BaseURL}}/deltaweb/hmi_login.asp"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - "Delta Controls ORCAview"

      - type: status
        status:
          - 200

