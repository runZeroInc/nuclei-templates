id: huly-panel

info:
  name: Huly Login Panel - Detect
  author: righettod
  severity: info
  description: |
    Huly products was detected.
  reference:
    - https://huly.io/
  metadata:
    verified: true
    max-request: 1
    shodan-query: http.html:"Huly"
    runzero-match: service["http.body"] matches "(?i)Huly"
  tags: panel,huly,login

http:
  - method: GET
    path:
      - "{{BaseURL}}/login%3Acomponent%3ALoginApp"

    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains(to_lower(body), "<title>huly</title>")'
        condition: and

