id: klog-server-default-login

info:
  name: KLog Server - Default Login
  author: s4e-io
  severity: high
  description: |
    KLog Server default login credentials were discovered.
  reference:
    - https://www.klogserver.com/
  metadata:
    max-request: 2
    vendor: klogserver
    product: klog_server
    runzero-match: any(each(service["html.titles"]), {# matches "KLog Server"})
  tags: default-login,klog-server

http:
  - raw:
      - |
        POST /actions/entree.php HTTP/1.1
        Host: {{Hostname}}
        Content-Type: application/x-www-form-urlencoded

        user={{username}}&pswd={{password}}&action=login
      - |
        GET /index.php HTTP/1.1
        Host: {{Hostname}}

    attack: pitchfork
    payloads:
      username:
        - admin
      password:
        - admin
    host-redirects: true
    max-redirects: 3
    matchers:
      - type: dsl
        dsl:
          - 'contains_all(body, "<title>KLog Server", "ADMIN", "Dashboard")'
          - 'status_code == 200'
        condition: and

